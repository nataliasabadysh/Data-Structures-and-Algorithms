// Creating a simple toggle using XOR:

let state = 0; // 0 for off, 1 for on
state = state ^ 1; // Toggle state
console.log(state); // 1

state = state ^ 1; // Toggle state again
console.log(state); // 0
